[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "profile-hooks"
version = "0.1.0"
description = "Configurable profile preflight hooks and validation"
dependencies = ["pytest>=7.0", "pyyaml>=6.0"]
